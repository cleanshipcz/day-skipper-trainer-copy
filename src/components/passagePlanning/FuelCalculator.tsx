import { useState } from "react";
import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";
import { calculatePassage, formatDuration, formatEta, validatePassageInput, type PassageCalculation } from "@/features/passagePlanning/calculations"; import { useProgress } from "@/hooks/useProgress"; import { TOPIC_IDS } from "@/constants/topicRegistry";
const scenarios=[{name:"Harbour hop",distanceNm:18,speedKnots:6,fuelLitresPerHour:2},{name:"Channel crossing",distanceNm:72,speedKnots:7,fuelLitresPerHour:3.5}];
export function FuelCalculator(){
 const [values,setValues]=useState({distanceNm:18,speedKnots:6,fuelLitresPerHour:2,reservePercent:20,departureTime:""}); const [result,setResult]=useState<PassageCalculation|null>(null); const [submitted,setSubmitted]=useState(values); const {saveProgress}=useProgress();
 const errors=validatePassageInput(values);
 const field=(key:keyof typeof values,label:string,type="number")=><div><Label htmlFor={key}>{label}</Label><Input id={key} type={type} min="0" value={values[key]} onChange={e=>setValues(v=>({...v,[key]:type==="number"?Number(e.target.value):e.target.value}))}/></div>;
 const calculate=()=>{ if(errors.length)return; const input={...values,departureTime:values.departureTime?new Date(values.departureTime).toISOString():undefined}; setSubmitted(values); setResult(calculatePassage(input)); };
 return <div className="space-y-5"><div className="flex flex-wrap gap-2">{scenarios.map(s=><Button variant="outline" key={s.name} onClick={()=>setValues(v=>({...v,...s}))}>{s.name}</Button>)}</div><div className="grid sm:grid-cols-2 gap-4">{field("distanceNm","Distance (nautical miles)")}{field("speedKnots","Speed over ground (knots)")}{field("fuelLitresPerHour","Fuel consumption (litres/hour)")}{field("reservePercent","Reserve (%)")}{field("departureTime","Departure date and time (your local timezone)","datetime-local")}</div>{errors.map(e=><p role="alert" className="text-destructive" key={e}>{e}</p>)}<Button disabled={errors.length>0} onClick={calculate}>Calculate / update result</Button>{result&&<Card aria-live="polite"><CardHeader><CardTitle>Calculated plan</CardTitle></CardHeader><CardContent className="space-y-2"><p>Time: {submitted.distanceNm} nm ÷ {submitted.speedKnots} kn = <b>{formatDuration(result.durationMinutes)}</b> ({result.hours.toFixed(2)} decimal hours). Minutes are rounded to the nearest minute.</p><p>Base fuel: {result.hours.toFixed(2)} h × {submitted.fuelLitresPerHour} L/h = <b>{result.fuelLitres.toFixed(2)} L</b>.</p><p>Reserve: {result.fuelLitres.toFixed(2)} L × {submitted.reservePercent}% = <b>{result.reserveLitres.toFixed(2)} L</b>.</p><p>Exact estimated total: <b>{result.fuelWithReserveLitres.toFixed(2)} L</b>. Conservative practical carry: <b>{result.practicalFuelLitres} L</b> (rounded up to the next whole litre; never down). Confirm tank capacity, usable fuel and vessel limits separately.</p><p>ETA: <b>{result.eta?formatEta(result.eta):"Choose a departure time"}</b>. The timezone and UTC offset are explicit; the calculated instant is preserved across locale and daylight-saving changes.</p></CardContent></Card>}<Button disabled={!result} onClick={()=>void saveProgress(TOPIC_IDS.PASSAGE_PLANNING_CALCULATOR,true,100,10,{input:submitted})}>Complete calculation</Button></div>
}
