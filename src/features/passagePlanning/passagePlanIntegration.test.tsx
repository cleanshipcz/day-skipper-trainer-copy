import { fireEvent, render, screen } from "@testing-library/react"; import { beforeEach, describe, expect, it, vi } from "vitest"; import { PassagePlanBuilder } from "@/components/passagePlanning/PassagePlanBuilder";
const saveProgress=vi.fn().mockResolvedValue(true); vi.mock("@/hooks/useProgress",()=>({useProgress:()=>({saveProgress})}));
describe("passage plan flow",()=>{beforeEach(()=>{localStorage.clear();saveProgress.mockClear()});it("builds and saves a plan",async()=>{render(<PassagePlanBuilder/>);fireEvent.change(screen.getByLabelText("Plan name"),{target:{value:"Test passage"}});fireEvent.click(screen.getByRole("button",{name:"Add waypoint"}));fireEvent.click(screen.getByRole("button",{name:"Save & complete plan"}));expect(localStorage.getItem("day-skipper-passage-plan")).toContain("Test passage");expect(saveProgress).toHaveBeenCalledWith("passage-planning-builder",true,100,15,expect.any(Object))})});
